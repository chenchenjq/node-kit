# SKU 编码使用有界且不可复用的序号

每个 SPU 首次持久化时冻结宿主 SPU 编码，SKU 编码由该快照加 `00` 至 `99` 组成；注册 SPU 编码在作用域内永久唯一，不能在另一个 SPU 上复用。宿主编码按原值区分大小写，长度为 1 至 126 个字符，拒绝首尾空白、控制字符和换行，但不进行 Unicode 或大小写改写。`99` 是第 100 个可分配序号，之后的新身份失败。已经持久化的序号即使对应 SKU 被归档也永不释放或改写，以保持外部映射和历史记录稳定；代价是每个 SPU 终身最多拥有 100 个不同 SKU 身份。
