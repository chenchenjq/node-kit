# 草稿不落库且模式转换保留历史身份

打开编辑器、读取和预览都不会创建 SKU 或占用编号；首次保存无规格配置才创建默认 SKU，首次保存多规格配置则直接从真实组合开始编号。默认与多规格之间转换会归档旧组合并创建或显式恢复原身份，不自动复制或汇总库存，价格和图片也只有在明确指定来源与目标时才复制。重新出现的历史组合必须由请求显式恢复原 SKU；改名保留规格身份，含义替换创建新身份；首版不提供 SKU、规格或整个作用域的物理删除 API。
