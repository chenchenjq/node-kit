# 结构、SKU 配置和库存使用独立并发版本

SPU 规格结构、每个 SKU 的价格图片状态、以及每条本地或外部库存事实分别使用结构版本、配置版本和库存版本令牌。启用与停用只改变 SKU 配置版本；归档与恢复既改变有效组合集合也改变 SKU 状态，因此同时校验并增加结构版本和对应配置版本。配置批量写入可以原子校验结构及所改 SKU 的预期版本，但库存只能通过独立命令修改；命令 ID 在默认七天的回执保留期内保证相同载荷的重试幂等，并拒绝同一 ID 携带不同载荷，宿主可以延长该窗口。
